<?php
/** @var Forecast $block */
use Vendor\Weather\Block\Forecast;
$weather = $block->getWeather();
?>
<div class="forecast">
    <div class="container">
        <div class="center">
            <h3><?= $weather['city'] ?></h3>
            <div class="text-center">
                <img src="<?= $weather['url'] ?>" alt="<?= $weather['description'] ?>" />
                <span id="temperature"><?= $weather['temp'] ?>°<span>
            </div>
            <div class="row">
                <p><?= $weather['main'] ?></p>
                <p>wind <?= $weather['wind'] ?> m/s</p>
            </div>
        </div>
    </div>
    <div class="card-section">
        <div class="center">
            <div class="time-box">
                <?php foreach ($block->getForecast() as $index => $data): ?>
                <?php if ($index === 0): ?>
                    <div class="col first">
                        <p><?= $data['time']?></p>
                        <hr class="hr-title">
                        <p><img src="<?= $data['url'] ?>" alt="<?= $data['description'] ?>" /></p>
                        <p><span class="d-min-temp"><?= $data['temp_min'] ?>°</span> <span class="d-max-temp"><?= $data['temp_max'] ?>°</span></p>
                    </div>
                <?php else: ?>
                    <div class="col ">
                        <p><?= $data['time']?></p>
                        <hr class="hr-title">
                        <p><img src="<?= $data['url'] ?>" alt="<?= $data['description'] ?>" /></p>
                        <p><span class="d-min-temp"><?= $data['temp_min'] ?>°</span> <span class="d-max-temp"><?= $data['temp_max'] ?>°</span></p>
                    </div>
                <?php endif; ?>
                <?php endforeach; ?>
            </div>
        </div>
    </div>
</div>
<style>
    .container {
        background: #039be5;
        color: #fff;
        padding: 10px 15px;
        margin: 0 auto;
    }

    .center {
        margin-left: 25%;
        width: 50%;
    }

    #temperature {
        font-size: 54px;
        line-height: 1.7em;
        vertical-align: top;
    }

    .text-center {
        text-align: center;
    }

    .card-section {
        background-color: #e0e0e0;
    }

    .time-box {
        display: flex;
        color: #333;
        padding: 30px 0;
        text-align: center;
    }

    .time-box .col {
        min-height: 1px;
        padding: 0 15px;
        border-left: 1px solid #fff;
    }

    .time-box .col.first {
        border: none;
    }

    .hr-title {
        border-color: #039be5;
        border-width: 2px;
        max-width: 30%;
        margin: 15px auto;
    }

    .d-min-temp {
        color: #039be5;
    }

    .d-max-temp {
        color: #ef5350;
    }
</style>
